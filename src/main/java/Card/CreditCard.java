package Card;

public class CreditCard extends PaymentCard {

    double creditTotal

    public CreditCard(int cardNumber, String expiryDate, String customerName, String bankName, int sortCode, double creditTotal, double riskMultiplier) {
        super(cardNumber, expiryDate, customerName, bankName, sortCode);
        this.cred
    }
}
